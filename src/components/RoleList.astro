---
import type { Role, Profile } from '../data/cv';
import { sortBullets } from '../data/cv';

interface Props {
  title: string;
  roles: Role[];
  profile: Profile;
}

const { title, roles, profile } = Astro.props;
---

<section>
  <h2 class="section-title">{title}</h2>
  <hr class="section-divider" />
  {roles.map((role) => (
    <div class="role">
      <div class="role-header">
        <span class="role-company">
          {role.company}
          {role.companyNote && (
            <span class="role-company-note"> ({role.companyNote})</span>
          )}
        </span>
        <span class="role-location">{role.location}</span>
      </div>
      <div class="role-header">
        <span class="role-title">{role.title}</span>
        <span class="role-dates">{role.start} – {role.end}</span>
      </div>
      <ul class="role-bullets">
        {sortBullets(role.bullets, profile).map((bullet) => <li>{bullet.text}</li>)}
      </ul>
    </div>
  ))}
</section>
